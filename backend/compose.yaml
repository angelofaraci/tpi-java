services:
  postgres:
    image: 'postgres:latest'
    container_name: db
    environment:
      - 'POSTGRES_USER=myuser'
      - 'POSTGRES_PASSWORD=secret'
      - 'PGTZ=UTC'
      - 'TZ=UTC'
    ports:
      - '55443:5432'
